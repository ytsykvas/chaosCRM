p = t('devise.mailer.reset_password_instructions.greeting', email: @resource.email)

p = t('devise.mailer.reset_password_instructions.link_is_requested')
p= link_to t('devise.mailer.reset_password_instructions.link_to_change_password'),
        edit_password_url(@resource, reset_password_token: @token)

p = t('devise.mailer.reset_password_instructions.ignore')
p = t('devise.mailer.reset_password_instructions.hint')
